>@
Pedro Virissimo Scarelli
@<
main
f_total, i_prestacoes, i_pagas, f_valor;


write("qual o numero total de prestações: ");
read(i_prestacoes);
writeln("quantas prestacoes voce pagou: ");
read(i_pagas);
writeln("qual a o valor da prestacao: ");
read(f_valor);

f_total = (i_prestacoes - i_pagas) * f_valor;

write("saldo devedor: ");
write(f_total);
end